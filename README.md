# Game Engine Project - RunBoyRun

โปรเจคเกม Endless Runner ที่ใช้ OpenGL สำหรับแสดงตัวละครพร้อม Skeletal Animation

## 📋 คำอธิบายโปรเจค

โปรเจคนี้เป็นเกม Endless Runner ที่ตัวละครจะวิ่งไปข้างหน้าอย่างต่อเนื่อง โดยผู้เล่นสามารถควบคุมทิศทางและทำแอคชันต่างๆ ได้ เช่น กระโดดและกลิ้ง ใช้ระบบ Third-Person Camera ที่ติดตามตัวละครอัตโนมัติ

## ✨ คุณสมบัติ

- **Skeletal Animation**: รองรับการเล่น animation แบบ skeletal animation จากไฟล์ .dae
- **Third-Person Camera**: กล้องติดตามตัวละครแบบ third-person view
- **Endless Runner**: ตัวละครวิ่งไปข้างหน้าอย่างต่อเนื่อง
- **Animation States**: 
  - Running (วิ่ง)
  - Jumping (กระโดด)
  - Rolling (กลิ้ง)
- **Seamless Animation Looping**: Animation วิ่งจะวนลูปอย่างต่อเนื่องโดยไม่รีเซ็ต

## 🎮 การควบคุม

- **A**: หมุนตัวละครไปทางซ้าย
- **D**: หมุนตัวละครไปทางขวา
- **W**: กระโดด (กดค้าง)
- **S**: กลิ้ง (กดค้าง)
- **Scroll Wheel**: ซูมเข้า/ออก
- **ESC**: ปิดเกม

## 🛠️ เทคโนโลยีที่ใช้

- **OpenGL**: สำหรับการเรนเดอร์กราฟิก 3D
- **GLFW**: สำหรับการจัดการหน้าต่างและ input
- **GLAD**: สำหรับ OpenGL function loader
- **GLM**: สำหรับการคำนวณทางคณิตศาสตร์ (vectors, matrices)
- **Assimp**: สำหรับโหลดไฟล์โมเดลและ animation (.dae)
- **CMake**: สำหรับ build system

## 📁 โครงสร้างโปรเจค

```
GameEngProject-Palbomb-2.0-/
├── assets/              # ไฟล์โมเดลและ texture
│   ├── RunInPlace.dae  # Animation วิ่ง
│   ├── Jump.dae        # Animation กระโดด
│   ├── Roll.dae        # Animation กลิ้ง
│   └── textures/       # Texture files
├── shaders/            # Shader files
│   ├── anim_model.vs  # Vertex shader
│   └── anim_model.fs  # Fragment shader
├── src/                # Source code
│   ├── main.cpp       # Main game loop
│   ├── animator.h     # Animation controller
│   └── animator.cpp   # Animation implementation
├── external/          # External libraries
│   └── glad/         # GLAD library
└── CMakeLists.txt    # CMake configuration
```

## 🚀 วิธีติดตั้งและรัน

### ความต้องการของระบบ

- CMake 3.16 หรือสูงกว่า
- C++17 compiler
- OpenGL 3.3 หรือสูงกว่า

### ขั้นตอนการ Build

1. **Clone หรือดาวน์โหลดโปรเจค**

2. **สร้าง build directory และ build โปรเจค**:
```bash
cmake -S . -B build
cmake --build build
```

3. **รันโปรแกรม**:
```bash
cd build
./PlayableCharacter
```

### สำหรับ Windows

```bash
cmake -S . -B build
cmake --build build --config Release
cd build/Release
PlayableCharacter.exe
```

## 📝 รายละเอียดเทคนิค

### Animation System

โปรเจคใช้ระบบ Animation ที่สามารถ:
- เล่น animation หลายๆ ตัว (Running, Jumping, Rolling)
- สลับระหว่าง animation ได้อย่างราบรื่น
- รักษา animation time ของ Running animation เพื่อให้วนลูปได้อย่างต่อเนื่อง
- ตรวจจับเมื่อ animation จบ (wrap-around) เพื่อกลับไป Running state

### Camera System

- **Third-Person View**: กล้องอยู่ด้านหลังและด้านบนของตัวละคร
- **Fixed Angle**: กล้องถูกล็อคในมุมที่กำหนด (ไม่สามารถหมุนด้วยเมาส์ได้)
- **Auto Follow**: กล้องติดตามตัวละครอัตโนมัติ
- **Zoom Control**: สามารถซูมด้วย scroll wheel

### Movement System

- **Continuous Forward Movement**: ตัวละครวิ่งไปข้างหน้าอย่างต่อเนื่อง
- **Rotation Control**: สามารถหมุนตัวละครด้วย A/D
- **Action Triggers**: กด W/S เพื่อทำแอคชัน (กระโดด/กลิ้ง)

## 🎨 Assets

โปรเจคใช้ไฟล์ animation ในรูปแบบ .dae (COLLADA) และ texture files ในรูปแบบ PNG

### Animation Files
- `RunInPlace.dae`: Animation วิ่ง (วนลูป)
- `Jump.dae`: Animation กระโดด (เล่นครั้งเดียว)
- `Roll.dae`: Animation กลิ้ง (เล่นครั้งเดียว)

### Texture Files
- `Boy_Hair_diffuse.png`
- `Boy_Hands_diffuse.png`
- `Boy_Head_diffuse.png`
- `Boy_LowerBody_diffuse.png`
- `Boy_Scarf_diffuse.png`
- `Boy_Shoes_diffuse.png`
- `Boy_UpperBody_diffuse.png`

## 🔧 การปรับแต่ง

### ปรับระยะกล้อง
แก้ไขใน `src/main.cpp`:
```cpp
float cameraDistance = 4.0f;  // เปลี่ยนค่าตรงนี้
```

### ปรับมุมกล้อง
แก้ไขใน `src/main.cpp`:
```cpp
float cameraAngleY = glm::radians(25.0f);  // เปลี่ยนค่าตรงนี้
```

### ปรับความเร็วการวิ่ง
แก้ไขใน `src/main.cpp`:
```cpp
float runSpeed = 2.0f * deltaTime;  // เปลี่ยนค่าตรงนี้
```

## 📄 License

โปรเจคนี้ใช้ไลบรารีภายนอกหลายตัวที่มี license ต่างกัน:
- GLFW: Zlib/libpng license
- GLM: MIT license
- Assimp: BSD 3-Clause license
- GLAD: Public domain

## 👨‍💻 ผู้พัฒนา

โปรเจคนี้เป็นส่วนหนึ่งของ Game Engine Project

## 🙏 ขอบคุณ

- LearnOpenGL สำหรับ tutorial และ code examples
- Joey de Vries สำหรับ LearnOpenGL resources

